{
  "name": "root",
  "private": true,
  "scripts": {
    "verify": "node scripts/verify-version.js",
    "verdaccio": "verdaccio",
    "publish": "lerna publish --no-private --registry http://localhost:4873/"
  },
  "devDependencies": {
    "lerna": "^6.4.1"
  },
  "workspaces": [
    "packages/*"
  ],
  "dependencies": {
    "verdaccio": "5.20.1"
  }
}
